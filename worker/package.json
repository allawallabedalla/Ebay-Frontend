{
  "name": "kleinanzeigen-suchassistent-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker Backend-Proxy fuer die Kleinanzeigen-Suche (Claude).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
